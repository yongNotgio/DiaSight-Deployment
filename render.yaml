services:
  - type: web
    name: diasight-dr-predictor
    env: python
    buildCommand: "pip install -r requirements.txt"
  startCommand: "python -m uvicorn main:app --host 0.0.0.0 --port 10000"
    plan: free
    autoDeploy: true
